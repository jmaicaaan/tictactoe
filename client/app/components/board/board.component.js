import template from './board.pug';
import controller from './board.controller';
import './board.scss';

let boardComponent = {
  bindings: {},
  template,
  controller
};

export default boardComponent;
